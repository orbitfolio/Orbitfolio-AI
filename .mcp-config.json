{
    "mcpServers": {
        "jules": {
            "command": "npx",
            "args": [
                "@amitdeshmukh/google-jules-mcp"
            ],
            "env": {
                "JULES_API_KEY": "${JULES_API_KEY}",
                "JULES_API_URL": "${JULES_API_URL}"
            }
        }
    }
}